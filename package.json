{
  "name": "timicons",
  "displayName": "Timicons",
  "version": "2.0.0-beta",
  "description": "File type icons for your favorite editor",
  "author": "Tim G. Thomas <tim@timgthomas.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "npm run clean && npm run export && npm run build",
    "clean": "rm -rf dist tmp && mkdir dist",
    "export": "sketchtool export layers src/timicons.sketch --output=./tmp",
    "build": "fantasticon tmp/ -o dist -n timicons -t ttf -g json && cp -r ./dist/timicons.ttf ./extension/timicons.ttf",
    "install": "ln -s $(pwd) ~/.vscode/extensions/timicons"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/timgthomas/timicons.git"
  },
  "devDependencies": {
    "fantasticon": "^1.2.3"
  },
  "contributes": {
    "iconThemes": [
      {
        "id": "timicons",
        "label": "Timicons",
        "path": "./extension/timicons-icon-theme.json"
      }
    ]
  },
  "engines": {
    "vscode": "*"
  }
}
